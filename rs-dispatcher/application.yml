server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/samierp_mepso?useSSL=false&serverTimezone=UTC
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  # H2 console not used when using MySQL
